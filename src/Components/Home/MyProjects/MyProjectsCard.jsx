import { Link } from "react-router-dom";

const MyProjectsCard = ({ data }) => {
    const { name, shortInfo, img, liveLink, clientLink, serverLink, feature, techIcon } = data;
    return (
        <div>
            
        </div>
    );
};

export default MyProjectsCard;