import SectionTitle from "../../Shared/SectionTitle";

const AboutMe = () => {
    return (
        <div>
            <SectionTitle
                heading={"Skills & About Me"}
            ></SectionTitle>
        </div>
    );
};

export default AboutMe;